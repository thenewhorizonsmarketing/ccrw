export interface NavItem {
    label: string;
    href: string;
}

export interface Event {
    id: number;
    title: string;
    date: string;
    location: string;
    description: string;
}

export interface Committee {
    title: string;
    description: string;
}